.qz.fullscreen.abs
  .btn.abs#btn-2
  .btn.abs#btn-3
  img#logo.abs(src="img/logo.svg" alt="First")
  .banner
    .timer
      .left
        span Питання
        .questions
          span#current 1
          | &nbsp;
          span з
          | &nbsp;
          span#total 5
      .loader
        svg.abs
          circle(cx="32" cy="32" r="32")
        svg.abs#progress
          circle(cx="32" cy="32" r="32")        
        .sec.abs
          span#sec 30
          span секунд
    .task
    img.metafan.abs(src="/img/metafan.png")
  .game-zone.abs
    .game
      img.gates(src="/img/gates.png")
      .goalkeeper.abs
        .picture
      img.ball.abs(src="/img/ball.png")
      img.ball-clear.abs(src="/img/ball_clear.png")
      .shadow
    .quiz
      .quiz-result
      .quiz-item
        span
      .quiz-item
        span
      .quiz-item
        span
      .quiz-item
        span
